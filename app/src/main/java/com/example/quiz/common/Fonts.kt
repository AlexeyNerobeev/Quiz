package com.example.quiz.common

import androidx.compose.ui.text.font.Font
import androidx.compose.ui.text.font.FontFamily
import com.example.quiz.R

val leagueSpartanLight = FontFamily(
    Font(
       resId = R.font.league_spartan_light
    )
)

val leagueSpartanRegular = FontFamily(
    Font(
        resId = R.font.league_spartan_regular
    )
)

val leagueSpartanSemiBold = FontFamily(
    Font(
        resId = R.font.league_spartan_semibold
    )
)

val poppinsMedium = FontFamily(
    Font(
        resId = R.font.poppins_medium
    )
)

val poppinsSemiBold = FontFamily(
    Font(
        resId = R.font.poppins_semibold
    )
)

val poppinsRegular = FontFamily(
    Font(
        resId = R.font.poppins_regular
    )
)

val robotoRegular = FontFamily(
    Font(
        resId = R.font.roboto_regular
    )
)

val robotoBlack = FontFamily(
    Font(
        resId = R.font.roboto_black
    )
)